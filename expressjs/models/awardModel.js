class Award {
  constructor(id, codigo, etiqueta, precio, puntos) {
    this.id = id;
    this.codigo = codigo;
    this.etiqueta = etiqueta;
    this.precio = precio;
    this.puntos = puntos;
  }
}

module.exports = Award;
